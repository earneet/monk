# 项目级 CLAUDE.md 创建 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在项目根创建 `CLAUDE.md`,写入已批准的项目指导内容,作为 AI 助手在本项目工作时的项目级指令。

**Architecture:** 单文件文档创建,无代码逻辑。内容严格取自设计规范 `docs/project/2026-07-08-guidance-docs-design.md` 第 4 节(已批准),不得改动措辞。

**Tech Stack:** Markdown(Godot 4.7 / GDScript 项目的约定文档)

## Global Constraints

(源自 spec,逐条遵守)
- 目标文件路径:`C:/Users/earne/workspace/monk/CLAUDE.md`(项目根)
- 文件编码:UTF-8;行尾:LF(项目 `.editorconfig` charset=utf-8、`.gitattributes` `* text=auto eol=lf`)
- 文档语言:中文
- 引擎与语言事实:Godot 4.7 stable、纯 GDScript(单一语言)
- 内容唯一来源:spec 第 4 节;不得自行改写或新增条目
- 版本控制:项目当前**非 git 仓库**;提交步骤为**条件性**(见 Task 1 Step 4)

## File Structure

- **Create:** `CLAUDE.md`(项目根)—— 单一职责:项目级 AI 指导文档。无其他文件改动。

---

### Task 1: 创建项目级 CLAUDE.md

**Files:**
- Create: `CLAUDE.md`(项目根)

**Interfaces:**
- Consumes: spec 第 4 节已批准内容(下方 Step 1 完整内联,无需查阅其他文件)
- Produces: 项目根 `CLAUDE.md`,供 AI 助手后续在本项目工作时自动加载

- [ ] **Step 1: 创建 `CLAUDE.md` 并写入完整内容**

创建 `C:/Users/earne/workspace/monk/CLAUDE.md`,写入以下完整内容(与 spec 第 4 节逐字一致):

````markdown
# monk —— 一休扫地

休闲益智类「一笔画」游戏。玩家操控小和尚,需扫过寺院中每一寸可通行土地,
且不可重复踏上已扫过的格子(本质是网格上的哈密顿路径问题)。

核心机制:
- 障碍:流水、假山 —— 不可通行、免打扫
- 门:可穿行;部分门需先踩机关才能开启
- 机制会持续扩展(传送、桥、动态水等),故机制系统设计为数据驱动、可插拔

当前阶段:原型期(仅 Scenes/boot.tscn,尚无业务代码)

## 技术栈

- 引擎:Godot 4.7(stable)
- 语言:纯 GDScript(单一语言,不混用其他脚本语言)
- 渲染管线:Forward+
- 注:addons/py4godot 为早期实验遗留,非本项目所用,待移除(见「已知技术债」)

## 目录结构

类型分层组织:

    monk/
    ├── project.godot
    ├── scenes/            # 所有 .tscn(按子系统命名:boot / grid / level / ui …)
    ├── scripts/           # 所有 .gd,按子系统分子目录
    │   ├── grid/          # 网格模型、瓦片类型、路径校验
    │   ├── mechanics/     # 各类机制,每个机制一个 .gd
    │   ├── level/         # 关卡加载、进度、胜负判定
    │   └── ui/            # HUD、菜单
    ├── resources/         # 关卡数据 .tres
    ├── assets/{art,audio,fonts}
    └── addons/            # 第三方插件(py4godot 待移除)

约定:场景入 scenes/,脚本入 scripts/ 对应子目录;脚本与同名场景配对。

## 架构原则

- 机制数据驱动:每种地形/机关 = 一份数据定义(Resource/脚本)+ 一段规则逻辑,
  新增机制不改主循环。
- 逻辑/表现分离:规则逻辑(能否通行、触发什么)独立于渲染表现(贴图、动画),
  便于测试与替换。
- 关卡数据用 Resource(.tres):关卡布局与机制配置序列化为 .tres,可在编辑器
  可视化编辑、利于版本管理。
- 单一职责:每个脚本只做一件事;文件膨胀即为重构信号。

## 代码规范

- 命名:变量/函数 snake_case;类名/节点名 PascalCase;常量 UPPER_SNAKE;
  私有成员加 _ 前缀。
- 类型:尽量使用静态类型注解(var x: int = 0、func f() -> void:)。
- 注释:严格承接全局 CLAUDE.md —— 默认不加注释,只在必要时解释「为什么」
  (业务规则、非常规操作、复杂算法);每函数不超过 3 行注释;禁止「描述做什么」
  的注释。
- 每个脚本用 class_name 暴露类型,避免全局命名冲突。

## 运行与构建

- 运行:编辑器 F5(主场景 boot.tscn);命令行 godot --path .
- 导出:目标平台待定(默认桌面)
- 版本控制:项目尚未 git init(见「已知技术债」);.godot/ 已在 .gitignore

## 已知技术债

- py4godot 待移除:addons/py4godot 含 2600+ cpython 文件,应整体删除
- Godot 4.4 → 4.7 升级:project.godot 当前 config/features=4.4,需用 4.7
  打开并保存以升级,随后回归基本功能
- git 未初始化:建议尽快 git init 并提交,建立版本基线
````

- [ ] **Step 2: 验证文件已创建且内容完整**

Run:
```bash
cd "C:/Users/earne/workspace/monk" && grep -E "^(# monk|## 技术栈|## 目录结构|## 架构原则|## 代码规范|## 运行与构建|## 已知技术债)" CLAUDE.md
```
Expected: 输出 **7 行**(1 个一级标题 `# monk` + 6 个二级标题),全部命中。

- [ ] **Step 3: 验证编码与行尾**

Run:
```bash
cd "C:/Users/earne/workspace/monk" && file CLAUDE.md && file --mime-encoding CLAUDE.md
```
Expected: 文本文件,编码为 `utf-8`;行尾为 LF(`grep -U -c $'\r' CLAUDE.md` 应为 `0`,无 CRLF)。

- [ ] **Step 4: 提交版本控制(条件性)**

判断:
- **若项目已是 git 仓库**(`git rev-parse --is-inside-work-tree` 成功):
  ```bash
  cd "C:/Users/earne/workspace/monk" && git add CLAUDE.md docs/project/2026-07-08-guidance-docs-design.md docs/superpowers/plans/2026-07-08-claude-md.md && git commit -m "docs: add project-level CLAUDE.md

  Co-Authored-By: Claude <noreply@anthropic.com>"
  ```
- **若项目尚未 git init(当前现状)**:**跳过本步**,仅在执行报告中注明"未提交(项目非 git 仓库);`git init` 已列为技术债"。不要擅自 `git init`——该时机由用户决定。

---

## 备注:关联技术债(不在本计划内执行)

下列为独立任务,需用户另行确认后再处理,本计划**不**涵盖:
- 移除 `addons/py4godot`(实验遗留)
- Godot 4.4 → 4.7 升级(`project.godot` 的 `config/features`)
- `git init` 建立版本基线
